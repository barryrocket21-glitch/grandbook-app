// TODO Phase 4: refactor detail order untuk schema baru.
// Akan tampilkan: status_history, channel + rate_snapshot, alamat
// struktural, link ke wilayah_id master_wilayah.
import { RefactorBanner } from '@/components/ui/refactor-banner'
export default function OrderDetailPage() {
  return <RefactorBanner phase="Phase 4 (Detail Order + Status History)" pageTitle="Detail Order" />
}
